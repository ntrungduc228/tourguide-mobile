import {View, Text} from 'react-native';
import React from 'react';
import {List} from 'react-native-paper';

type DestinationitemProps = {};

export const DestinationItem = ({}: DestinationitemProps) => {
  return (
    <View>
      <View></View>
    </View>
  );
};

export default DestinationItem;
