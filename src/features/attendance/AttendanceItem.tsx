import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

export const AttendanceItem = (props: Props) => {
  return (
    <View>
      <Text>AttendanceItem</Text>
    </View>
  );
};

export default AttendanceItem;
