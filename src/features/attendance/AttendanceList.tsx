import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

export const AttendanceList = (props: Props) => {
  return (
    <View className="bg-white">
      <Text>AttendanceList</Text>
    </View>
  );
};

export default AttendanceList;
